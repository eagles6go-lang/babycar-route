# 👶🛗 ベビカルート

ベビーカー利用者向けの乗換ルート検索(関東圏)。
「どのルートが楽か」「エレベーターはあるか」「おむつ替えできるトイレはどこか・きれいか」「何号車に乗ると良いか」がわかることを目指したWebアプリです。

## 使い方(ローカル)

静的サイトなので、HTTPサーバーで配信するだけで動きます。

```
cd このフォルダ
python -m http.server 8642
# → http://localhost:8642 を開く
```

## GitHub Pages で公開する

1. GitHubで新しいリポジトリを作成(公開前提なら public)
2. このフォルダをpush
   ```
   git remote add origin https://github.com/<ユーザー名>/<リポジトリ名>.git
   git push -u origin main
   ```
3. リポジトリの Settings → Pages → Source を「Deploy from a branch」、Branch を `main` / `(root)` に設定
4. 数分後 `https://<ユーザー名>.github.io/<リポジトリ名>/` で公開されます

スマホのブラウザでそのURLを開けば外出先で使えます(ホーム画面に追加推奨)。

## 機能

- **ルート検索**: 関東1,952駅・157路線の内蔵ネットワークで乗換検索。
  乗換回数のペナルティと乗換駅のバリアフリー情報を加味した「楽さ優先」のルートを最大3案表示。
  新幹線・特急専用路線(成田エクスプレス等)は候補から除外しています。
- **所要時間は目安**: 駅間距離ベースの概算です。正確な時刻は各ルートの
  「Yahoo!乗換で時刻を見る」/「駅すぱあとで見る」リンクで確認してください。
- **駅の詳細**: エレベーター・おむつ替えトイレ(きれいさ★)・おすすめ号車・注意点を表示。
  シードデータは `data/facilities.json`(参考情報・要現地確認)。
- **乗換ナビ(🧭)**: 駅ごとに「何号車に乗る→どのエレベーターで何階から何階へ→どう歩く」の
  ステップと、階構造のアイソメトリック断面図を表示。
  「🧊 3Dで見る」で指で回転・ピンチズームできる3D表示(three.js)に切替可能。
  データは `facilities.json` の `floors` / `transferGuides` で駅ごとに追加できる。
  EV前の号車位置は `steps[].atCar`(号車番号)を設定するとシャフトがその号車上に描画される。
- **口コミ・メモ**: 駅ごとに「トイレのきれいさ」「移動しやすさ」の★評価、号車メモ、自由メモを記録。
  ブラウザのlocalStorageに保存され、設定からJSONでエクスポート/インポートできます。
  → 良い情報が貯まったら `data/facilities.json` に反映していく運用を想定。
- **駅すぱあと連携(任意)**: [駅すぱあとAPI フリープラン](https://api-info.ekispert.com/form/free/)の
  アクセスキーを設定(⚙️)に登録すると、公式の経路検索結果ページを開けます。

## データの更新

駅・路線データを更新するには:

```
node tools/build_network.mjs
```

`tools/.cache` にダウンロードキャッシュが作られます。

## データ出典・注意

- 駅・路線データ: [station_database](https://github.com/Seo-4d696b75/station_database) (CC BY 4.0)
- 3D表示: [three.js](https://threejs.org/) r128 (MIT) を `vendor/` に同梱
- `data/facilities.json` のバリアフリー情報はシード(参考)情報です。**正確性は保証されません。
  必ず各鉄道会社の公式駅構内図・[らくらくおでかけネット](https://www.ecomo-rakuraku.jp/)で確認してください。**
- 徒歩連絡(駅名が異なる乗換)は `data/walk_transfers.json` で手動管理しています。

## 将来の一般公開に向けたTODO

- 口コミのサーバー保存(現状は端末ローカルのみ)
- ODPT(公共交通オープンデータセンター)の駅施設データ自動取り込み
- 号車位置情報の充実(東京メトロ系はオープンデータあり)
- 施設情報の出典明記と定期更新
